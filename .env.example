# Leave empty to use the Vite dev proxy (/api -> VITE_API_PROXY)
VITE_API_URL=
# Where the Vite dev proxy forwards /api during `npm run dev`
VITE_API_PROXY=http://localhost:8000
# Set to "true" to run the UI on placeholder data with no backend
VITE_USE_MOCK=false
